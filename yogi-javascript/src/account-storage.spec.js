describe('AccountStorage', () => {
    let session;

    beforeAll(() => {
        session = new yogi.Session();
    });

    afterAll(() => {
        session.close();
    });

    xit('should allow storing and reading data', (done) => {
    });

    xit('should allow storing and reading data', (done) => {
    });

    xit('should reject storing and reading data when not logged in', (done) => {
    });
});
